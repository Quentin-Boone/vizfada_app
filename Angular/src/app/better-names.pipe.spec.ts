import { BetterNamesPipe } from './better-names.pipe';

describe('BetterNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new BetterNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
